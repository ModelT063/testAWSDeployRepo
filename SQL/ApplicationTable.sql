CREATE TABLE `Team20Schema`.`Application` (
  `Application_ID` INT NOT NULL,
  `Application_Time` DATETIME NULL,
  `Application_Status` VARCHAR(45) NULL,
  `Application_Document` BLOB NULL,
  PRIMARY KEY (`Application_ID`));
